        {/* Product Details */}
          <div className="space-y-8">
            {/* Breadcrumb */}
            <div className="text-sm text-gray-500 font-medium">
              <span className="hover:text-[#131313] cursor-pointer">{product.department?.department_name}</span>
              <span className="mx-2">•</span>
              <span className="hover:text-[#131313] cursor-pointer">{product.subcategory?.subcategory_name}</span>
              {product.subcategory?.categorysubcategory?.[0]?.category?.category_name && (
                <>
                  <span className="mx-2">•</span>
                  <span className="hover:text-[#131313] cursor-pointer">
                    {product.subcategory.categorysubcategory[0].category.category_name}
                  </span>
                </>
              )}
            </div>

            {/* Product Title */}
            <div className="space-y-3">
              <h1 className="text-4xl font-bold text-[#131313] leading-tight">{product.name}</h1>
              {product.brands && product.brands.length > 0 && (
                <div className="flex items-center gap-2">
                  <span className="text-lg text-gray-600">by</span>
                  <span className="text-lg font-semibold text-[#131313] bg-gray-100 px-3 py-1 rounded-full">
                    {product.brands}
                  </span>
                </div>
              )}
            </div>

            {/* Price & Condition */}
            <div className="flex items-center gap-4">
              <span className="text-5xl font-bold text-[#131313]">
                {formatPrice(product.price)}
              </span>
              <span className={`px-4 py-2 rounded-full text-sm font-semibold ${
                product.condition === 'New' 
                  ? 'bg-green-100 text-green-800' 
                  : 'bg-amber-100 text-amber-800'
              }`}>
                {formatString(product.condition)}
              </span>
            </div>


            {/* Description */}
            {product.decsription && (
              <div className="space-y-3 bg-white p-6 rounded-2xl shadow-sm border border-gray-200">
                <h3 className="text-xl font-semibold text-[#131313]">Description</h3>
                <p className="text-gray-700 leading-relaxed">{product.decsription}</p>
              </div>
            )}

            {/* Sizes */}
            {/* {
              product.sizes
            } */}

            {/* Add to Cart */}
            <div className="space-y-4 pt-4">
              <button 
                disabled={!product.is_available}
                className={`w-full py-5 rounded-2xl font-bold text-lg transition-all duration-200 flex items-center justify-center gap-3 ${
                  product.is_available
                    ? 'bg-[#131313] text-white hover:bg-gray-800 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5'
                    : 'bg-gray-300 text-gray-500 cursor-not-allowed'
                }`}
              >
                <ShoppingCart size={20} />
                {product.is_available ? 'Add to Cart' : 'Out of Stock'}
              </button>
              
              {product.is_available && (
                <button className="w-full py-5 border-2 border-[#131313] text-[#131313] rounded-2xl font-bold text-lg hover:bg-[#131313] hover:text-white transition-all duration-200 shadow-sm hover:shadow-lg">
                  Buy Now
                </button>
              )}
            </div>

            {/* Features */}
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 pt-6">
              <div className="flex items-center gap-4 bg-white p-4 rounded-xl shadow-sm border border-gray-200">
                <Truck size={24} className="text-[#131313]" />
                <div>
                  <p className="font-semibold text-[#131313]">Free Delivery</p>
                  <p className="text-sm text-gray-600">Orders over R500</p>
                </div>
              </div>
              <div className="flex items-center gap-4 bg-white p-4 rounded-xl shadow-sm border border-gray-200">
                <Shield size={24} className="text-[#131313]" />
                <div>
                  <p className="font-semibold text-[#131313]">Secure Payment</p>
                  <p className="text-sm text-gray-600">SSL encrypted</p>
                </div>
              </div>
            </div>

            {/* Vendor Info */}
            <div className="bg-white rounded-2xl p-6 shadow-lg border border-gray-200 space-y-4">
              <h3 className="text-xl font-semibold text-[#131313]">Sold by</h3>
              <div className="flex items-center gap-4">
                <img
                  src={product.vendor.avatar_url || "/api/placeholder/60/60"}
                  alt={product.vendor.store_name}
                  className="w-16 h-16 rounded-full bg-gray-100 border-2 border-gray-200"
                />
                <div className="flex-1">
                  <p className="font-bold text-[#131313] text-lg">{product.vendor.store_name}</p>
                  <p className="text-sm text-gray-600">
                    Last active: {formatTimeAgo(product.vendor.last_active)}
                  </p>
                  <div className="flex items-center gap-1 mt-1">
                    <Star size={14} className="fill-yellow-400 text-yellow-400" />
                    <span className="text-sm text-gray-600">4.9 • 1.2k reviews</span>
                  </div>
                </div>
              </div>
              <button className="w-full py-3 border-2 border-[#131313] text-[#131313] rounded-xl hover:bg-[#131313] hover:text-white transition-all duration-200 font-semibold flex items-center justify-center gap-2">
                <Eye size={18} />
                View Store
              </button>
            </div>

            {/* Product Info */}
            <div className="text-sm text-gray-500 bg-gray-100 p-4 rounded-xl">
              <p><strong>Product ID:</strong> {product.product_id}</p>
              <p><strong>Listed:</strong> {formatTimeAgo(product.created_at)}</p>
              <p><strong>Availability:</strong> {product.is_available ? 'In Stock' : 'Out of Stock'}</p>
            </div>
          </div>